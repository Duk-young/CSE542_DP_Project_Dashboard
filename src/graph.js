import React from 'react';
import { Line } from 'react-chartjs-2';

const StatisticalGraph = ({ data }) => {
  // Prepare data for the graph
  const graphData = {
    // Define labels and datasets
  };

  return <Line data={graphData} />;
};

export default StatisticalGraph;
